1. **Клонирование репозитория**: 
   - Репозиторий был клонирован на локальный компьютер с помощью команды `git clone <URL>`.

2. **Настройка Git**:
   - Были установлены глобальные настройки имени и e-mail пользователя с помощью команд:
     - `git config --global user.name "Ваше Имя"`
     - `git config --global user.email "your_email@example.com"`

3. **Проверка работы Git**:
   - Команды `git status` и `git log` были выполнены для проверки состояния репозитория. Они работают корректно.

## 2.3. Структура для семинара S01

1. **Создание папок**:
   - В корне репозитория была создана папка `homeworks/` (если её не было).
   - Внутри `homeworks/` была создана папка `HW01/`.

2. **Создание файла**:
   - В папке `homeworks/HW01/` был создан файл `HW01.ipynb`.

## 2.4. Содержание файла с NumPy

В файл `HW01.ipynb` был добавлен код, включающий:

- Импорт NumPy.
- Примеры создания массивов из списков и с использованием функций `zeros`, `ones`, `arange`.
- Демонстрация атрибутов массива (форма и тип данных).
- Примеры индексирования и срезов.
- Примеры векторных операций (поэлементное сложение) и базовой статистики (среднее значение).

Код снабжён комментариями, поясняющими выполняемые операции.

## Заключение

Все задания выполнены в соответствии с требованиями. Структура репозитория соответствует заданной, код в ноутбуке работает без ошибок (при установленной библиотеке NumPy).
